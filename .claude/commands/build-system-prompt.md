---
description: Build the system-prompt module from spec/system-prompt.md
---

Read `src/system-prompt/PROMPT.md` and follow it exactly.

If `spec/system-prompt.md` does not exist, stop and tell me to run
`/harness-interview system-prompt` first. Do not infer the spec from the code, and do not
build a generic version to be helpful - a generic system-prompt module is the thing this
repo already ships.

Extra arguments from me, if any: $ARGUMENTS
