---
description: Build the optional derived-memory module from spec/derive.md
---

Read `src/derive/PROMPT.md` and follow it exactly.

If `spec/derive.md` does not exist, stop and tell me to run
`/harness:interview derive` first. The module is optional and needs the event
log: if `spec/eventlog.md` does not exist either, say that the event log comes
first.

Before you write code, tell me the entity type, the policy file's path, which
model extracts, and which process runs the extractor.
